# Upload Your Real APK Here
# You can drag and drop your Android APK file directly into this `/public` directory.
# Name it `mediassist.apk` (or update `src/config/appConfig.ts` with your custom filename).
#
# When visitors click "Download MediAssist APK", Vite will automatically serve this file 
# directly to their browser for an immediate 1-click download!
